/* axios v0.19.2 | (c) 2020 by Matt Zabriskie */
!function (e, t) {
  "object" == typeof exports && "object" == typeof module ? module.exports = t() : "function" == typeof define && define.amd ? define([], t) : "object" == typeof exports ? exports.axios = t() : e.axios = t();
}(this, function () {
  return function (e) {
    function t(r) {
      if (n[r]) return n[r].exports;
      var o = n[r] = {
        exports: {},
        id: r,
        loaded: !1
      };
      return e[r].call(o.exports, o, o.exports, t), o.loaded = !0, o.exports;
    }

    var n = {};
    return t.m = e, t.c = n, t.p = "", t(0);
  }([function (e, t, n) {
    e.exports = n(1);
  }, function (e, t, n) {
    "use strict";

    function r(e) {
      var t = new s(e),
          n = i(s.prototype.request, t);
      return o.extend(n, s.prototype, t), o.extend(n, t), n;
    }

    var o = n(2),
        i = n(3),
        s = n(4),
        a = n(22),
        u = n(10),
        c = r(u);
    c.Axios = s, c.create = function (e) {
      return r(a(c.defaults, e));
    }, c.Cancel = n(23), c.CancelToken = n(24), c.isCancel = n(9), c.all = function (e) {
      return Promise.all(e);
    }, c.spread = n(25), e.exports = c, e.exports.default = c;
  }, function (e, t, n) {
    "use strict";

    function r(e) {
      return "[object Array]" === j.call(e);
    }

    function o(e) {
      return "undefined" == typeof e;
    }

    function i(e) {
      return null !== e && !o(e) && null !== e.constructor && !o(e.constructor) && "function" == typeof e.constructor.isBuffer && e.constructor.isBuffer(e);
    }

    function s(e) {
      return "[object ArrayBuffer]" === j.call(e);
    }

    function a(e) {
      return "undefined" != typeof FormData && e instanceof FormData;
    }

    function u(e) {
      var t;
      return t = "undefined" != typeof ArrayBuffer && ArrayBuffer.isView ? ArrayBuffer.isView(e) : e && e.buffer && e.buffer instanceof ArrayBuffer;
    }

    function c(e) {
      return "string" == typeof e;
    }

    function f(e) {
      return "number" == typeof e;
    }

    function p(e) {
      return null !== e && "object" == typeof e;
    }

    function d(e) {
      return "[object Date]" === j.call(e);
    }

    function l(e) {
      return "[object File]" === j.call(e);
    }

    function h(e) {
      return "[object Blob]" === j.call(e);
    }

    function m(e) {
      return "[object Function]" === j.call(e);
    }

    function y(e) {
      return p(e) && m(e.pipe);
    }

    function g(e) {
      return "undefined" != typeof URLSearchParams && e instanceof URLSearchParams;
    }

    function v(e) {
      return e.replace(/^\s*/, "").replace(/\s*$/, "");
    }

    function x() {
      return ("undefined" == typeof navigator || "ReactNative" !== navigator.product && "NativeScript" !== navigator.product && "NS" !== navigator.product) && "undefined" != typeof window && "undefined" != typeof document;
    }

    function w(e, t) {
      if (null !== e && "undefined" != typeof e) if ("object" != typeof e && (e = [e]), r(e)) for (var n = 0, o = e.length; n < o; n++) t.call(null, e[n], n, e);else for (var i in e) Object.prototype.hasOwnProperty.call(e, i) && t.call(null, e[i], i, e);
    }

    function b() {
      function e(e, n) {
        "object" == typeof t[n] && "object" == typeof e ? t[n] = b(t[n], e) : t[n] = e;
      }

      for (var t = {}, n = 0, r = arguments.length; n < r; n++) w(arguments[n], e);

      return t;
    }

    function E() {
      function e(e, n) {
        "object" == typeof t[n] && "object" == typeof e ? t[n] = E(t[n], e) : "object" == typeof e ? t[n] = E({}, e) : t[n] = e;
      }

      for (var t = {}, n = 0, r = arguments.length; n < r; n++) w(arguments[n], e);

      return t;
    }

    function S(e, t, n) {
      return w(t, function (t, r) {
        n && "function" == typeof t ? e[r] = C(t, n) : e[r] = t;
      }), e;
    }

    var C = n(3),
        j = Object.prototype.toString;
    e.exports = {
      isArray: r,
      isArrayBuffer: s,
      isBuffer: i,
      isFormData: a,
      isArrayBufferView: u,
      isString: c,
      isNumber: f,
      isObject: p,
      isUndefined: o,
      isDate: d,
      isFile: l,
      isBlob: h,
      isFunction: m,
      isStream: y,
      isURLSearchParams: g,
      isStandardBrowserEnv: x,
      forEach: w,
      merge: b,
      deepMerge: E,
      extend: S,
      trim: v
    };
  }, function (e, t) {
    "use strict";

    e.exports = function (e, t) {
      return function () {
        for (var n = new Array(arguments.length), r = 0; r < n.length; r++) n[r] = arguments[r];

        return e.apply(t, n);
      };
    };
  }, function (e, t, n) {
    "use strict";

    function r(e) {
      this.defaults = e, this.interceptors = {
        request: new s(),
        response: new s()
      };
    }

    var o = n(2),
        i = n(5),
        s = n(6),
        a = n(7),
        u = n(22);
    r.prototype.request = function (e) {
      "string" == typeof e ? (e = arguments[1] || {}, e.url = arguments[0]) : e = e || {}, e = u(this.defaults, e), e.method ? e.method = e.method.toLowerCase() : this.defaults.method ? e.method = this.defaults.method.toLowerCase() : e.method = "get";
      var t = [a, void 0],
          n = Promise.resolve(e);

      for (this.interceptors.request.forEach(function (e) {
        t.unshift(e.fulfilled, e.rejected);
      }), this.interceptors.response.forEach(function (e) {
        t.push(e.fulfilled, e.rejected);
      }); t.length;) n = n.then(t.shift(), t.shift());

      return n;
    }, r.prototype.getUri = function (e) {
      return e = u(this.defaults, e), i(e.url, e.params, e.paramsSerializer).replace(/^\?/, "");
    }, o.forEach(["delete", "get", "head", "options"], function (e) {
      r.prototype[e] = function (t, n) {
        return this.request(o.merge(n || {}, {
          method: e,
          url: t
        }));
      };
    }), o.forEach(["post", "put", "patch"], function (e) {
      r.prototype[e] = function (t, n, r) {
        return this.request(o.merge(r || {}, {
          method: e,
          url: t,
          data: n
        }));
      };
    }), e.exports = r;
  }, function (e, t, n) {
    "use strict";

    function r(e) {
      return encodeURIComponent(e).replace(/%40/gi, "@").replace(/%3A/gi, ":").replace(/%24/g, "$").replace(/%2C/gi, ",").replace(/%20/g, "+").replace(/%5B/gi, "[").replace(/%5D/gi, "]");
    }

    var o = n(2);

    e.exports = function (e, t, n) {
      if (!t) return e;
      var i;
      if (n) i = n(t);else if (o.isURLSearchParams(t)) i = t.toString();else {
        var s = [];
        o.forEach(t, function (e, t) {
          null !== e && "undefined" != typeof e && (o.isArray(e) ? t += "[]" : e = [e], o.forEach(e, function (e) {
            o.isDate(e) ? e = e.toISOString() : o.isObject(e) && (e = JSON.stringify(e)), s.push(r(t) + "=" + r(e));
          }));
        }), i = s.join("&");
      }

      if (i) {
        var a = e.indexOf("#");
        a !== -1 && (e = e.slice(0, a)), e += (e.indexOf("?") === -1 ? "?" : "&") + i;
      }

      return e;
    };
  }, function (e, t, n) {
    "use strict";

    function r() {
      this.handlers = [];
    }

    var o = n(2);
    r.prototype.use = function (e, t) {
      return this.handlers.push({
        fulfilled: e,
        rejected: t
      }), this.handlers.length - 1;
    }, r.prototype.eject = function (e) {
      this.handlers[e] && (this.handlers[e] = null);
    }, r.prototype.forEach = function (e) {
      o.forEach(this.handlers, function (t) {
        null !== t && e(t);
      });
    }, e.exports = r;
  }, function (e, t, n) {
    "use strict";

    function r(e) {
      e.cancelToken && e.cancelToken.throwIfRequested();
    }

    var o = n(2),
        i = n(8),
        s = n(9),
        a = n(10);

    e.exports = function (e) {
      r(e), e.headers = e.headers || {}, e.data = i(e.data, e.headers, e.transformRequest), e.headers = o.merge(e.headers.common || {}, e.headers[e.method] || {}, e.headers), o.forEach(["delete", "get", "head", "post", "put", "patch", "common"], function (t) {
        delete e.headers[t];
      });
      var t = e.adapter || a.adapter;
      return t(e).then(function (t) {
        return r(e), t.data = i(t.data, t.headers, e.transformResponse), t;
      }, function (t) {
        return s(t) || (r(e), t && t.response && (t.response.data = i(t.response.data, t.response.headers, e.transformResponse))), Promise.reject(t);
      });
    };
  }, function (e, t, n) {
    "use strict";

    var r = n(2);

    e.exports = function (e, t, n) {
      return r.forEach(n, function (n) {
        e = n(e, t);
      }), e;
    };
  }, function (e, t) {
    "use strict";

    e.exports = function (e) {
      return !(!e || !e.__CANCEL__);
    };
  }, function (e, t, n) {
    "use strict";

    function r(e, t) {
      !i.isUndefined(e) && i.isUndefined(e["Content-Type"]) && (e["Content-Type"] = t);
    }

    function o() {
      var e;
      return "undefined" != typeof XMLHttpRequest ? e = n(12) : "undefined" != typeof process && "[object process]" === Object.prototype.toString.call(process) && (e = n(12)), e;
    }

    var i = n(2),
        s = n(11),
        a = {
      "Content-Type": "application/x-www-form-urlencoded"
    },
        u = {
      adapter: o(),
      transformRequest: [function (e, t) {
        return s(t, "Accept"), s(t, "Content-Type"), i.isFormData(e) || i.isArrayBuffer(e) || i.isBuffer(e) || i.isStream(e) || i.isFile(e) || i.isBlob(e) ? e : i.isArrayBufferView(e) ? e.buffer : i.isURLSearchParams(e) ? (r(t, "application/x-www-form-urlencoded;charset=utf-8"), e.toString()) : i.isObject(e) ? (r(t, "application/json;charset=utf-8"), JSON.stringify(e)) : e;
      }],
      transformResponse: [function (e) {
        if ("string" == typeof e) try {
          e = JSON.parse(e);
        } catch (e) {}
        return e;
      }],
      timeout: 0,
      xsrfCookieName: "XSRF-TOKEN",
      xsrfHeaderName: "X-XSRF-TOKEN",
      maxContentLength: -1,
      validateStatus: function (e) {
        return e >= 200 && e < 300;
      }
    };
    u.headers = {
      common: {
        Accept: "application/json, text/plain, */*"
      }
    }, i.forEach(["delete", "get", "head"], function (e) {
      u.headers[e] = {};
    }), i.forEach(["post", "put", "patch"], function (e) {
      u.headers[e] = i.merge(a);
    }), e.exports = u;
  }, function (e, t, n) {
    "use strict";

    var r = n(2);

    e.exports = function (e, t) {
      r.forEach(e, function (n, r) {
        r !== t && r.toUpperCase() === t.toUpperCase() && (e[t] = n, delete e[r]);
      });
    };
  }, function (e, t, n) {
    "use strict";

    var r = n(2),
        o = n(13),
        i = n(5),
        s = n(16),
        a = n(19),
        u = n(20),
        c = n(14);

    e.exports = function (e) {
      return new Promise(function (t, f) {
        var p = e.data,
            d = e.headers;
        r.isFormData(p) && delete d["Content-Type"];
        var l = new XMLHttpRequest();

        if (e.auth) {
          var h = e.auth.username || "",
              m = e.auth.password || "";
          d.Authorization = "Basic " + btoa(h + ":" + m);
        }

        var y = s(e.baseURL, e.url);

        if (l.open(e.method.toUpperCase(), i(y, e.params, e.paramsSerializer), !0), l.timeout = e.timeout, l.onreadystatechange = function () {
          if (l && 4 === l.readyState && (0 !== l.status || l.responseURL && 0 === l.responseURL.indexOf("file:"))) {
            var n = "getAllResponseHeaders" in l ? a(l.getAllResponseHeaders()) : null,
                r = e.responseType && "text" !== e.responseType ? l.response : l.responseText,
                i = {
              data: r,
              status: l.status,
              statusText: l.statusText,
              headers: n,
              config: e,
              request: l
            };
            o(t, f, i), l = null;
          }
        }, l.onabort = function () {
          l && (f(c("Request aborted", e, "ECONNABORTED", l)), l = null);
        }, l.onerror = function () {
          f(c("Network Error", e, null, l)), l = null;
        }, l.ontimeout = function () {
          var t = "timeout of " + e.timeout + "ms exceeded";
          e.timeoutErrorMessage && (t = e.timeoutErrorMessage), f(c(t, e, "ECONNABORTED", l)), l = null;
        }, r.isStandardBrowserEnv()) {
          var g = n(21),
              v = (e.withCredentials || u(y)) && e.xsrfCookieName ? g.read(e.xsrfCookieName) : void 0;
          v && (d[e.xsrfHeaderName] = v);
        }

        if ("setRequestHeader" in l && r.forEach(d, function (e, t) {
          "undefined" == typeof p && "content-type" === t.toLowerCase() ? delete d[t] : l.setRequestHeader(t, e);
        }), r.isUndefined(e.withCredentials) || (l.withCredentials = !!e.withCredentials), e.responseType) try {
          l.responseType = e.responseType;
        } catch (t) {
          if ("json" !== e.responseType) throw t;
        }
        "function" == typeof e.onDownloadProgress && l.addEventListener("progress", e.onDownloadProgress), "function" == typeof e.onUploadProgress && l.upload && l.upload.addEventListener("progress", e.onUploadProgress), e.cancelToken && e.cancelToken.promise.then(function (e) {
          l && (l.abort(), f(e), l = null);
        }), void 0 === p && (p = null), l.send(p);
      });
    };
  }, function (e, t, n) {
    "use strict";

    var r = n(14);

    e.exports = function (e, t, n) {
      var o = n.config.validateStatus;
      !o || o(n.status) ? e(n) : t(r("Request failed with status code " + n.status, n.config, null, n.request, n));
    };
  }, function (e, t, n) {
    "use strict";

    var r = n(15);

    e.exports = function (e, t, n, o, i) {
      var s = new Error(e);
      return r(s, t, n, o, i);
    };
  }, function (e, t) {
    "use strict";

    e.exports = function (e, t, n, r, o) {
      return e.config = t, n && (e.code = n), e.request = r, e.response = o, e.isAxiosError = !0, e.toJSON = function () {
        return {
          message: this.message,
          name: this.name,
          description: this.description,
          number: this.number,
          fileName: this.fileName,
          lineNumber: this.lineNumber,
          columnNumber: this.columnNumber,
          stack: this.stack,
          config: this.config,
          code: this.code
        };
      }, e;
    };
  }, function (e, t, n) {
    "use strict";

    var r = n(17),
        o = n(18);

    e.exports = function (e, t) {
      return e && !r(t) ? o(e, t) : t;
    };
  }, function (e, t) {
    "use strict";

    e.exports = function (e) {
      return /^([a-z][a-z\d\+\-\.]*:)?\/\//i.test(e);
    };
  }, function (e, t) {
    "use strict";

    e.exports = function (e, t) {
      return t ? e.replace(/\/+$/, "") + "/" + t.replace(/^\/+/, "") : e;
    };
  }, function (e, t, n) {
    "use strict";

    var r = n(2),
        o = ["age", "authorization", "content-length", "content-type", "etag", "expires", "from", "host", "if-modified-since", "if-unmodified-since", "last-modified", "location", "max-forwards", "proxy-authorization", "referer", "retry-after", "user-agent"];

    e.exports = function (e) {
      var t,
          n,
          i,
          s = {};
      return e ? (r.forEach(e.split("\n"), function (e) {
        if (i = e.indexOf(":"), t = r.trim(e.substr(0, i)).toLowerCase(), n = r.trim(e.substr(i + 1)), t) {
          if (s[t] && o.indexOf(t) >= 0) return;
          "set-cookie" === t ? s[t] = (s[t] ? s[t] : []).concat([n]) : s[t] = s[t] ? s[t] + ", " + n : n;
        }
      }), s) : s;
    };
  }, function (e, t, n) {
    "use strict";

    var r = n(2);
    e.exports = r.isStandardBrowserEnv() ? function () {
      function e(e) {
        var t = e;
        return n && (o.setAttribute("href", t), t = o.href), o.setAttribute("href", t), {
          href: o.href,
          protocol: o.protocol ? o.protocol.replace(/:$/, "") : "",
          host: o.host,
          search: o.search ? o.search.replace(/^\?/, "") : "",
          hash: o.hash ? o.hash.replace(/^#/, "") : "",
          hostname: o.hostname,
          port: o.port,
          pathname: "/" === o.pathname.charAt(0) ? o.pathname : "/" + o.pathname
        };
      }

      var t,
          n = /(msie|trident)/i.test(navigator.userAgent),
          o = document.createElement("a");
      return t = e(window.location.href), function (n) {
        var o = r.isString(n) ? e(n) : n;
        return o.protocol === t.protocol && o.host === t.host;
      };
    }() : function () {
      return function () {
        return !0;
      };
    }();
  }, function (e, t, n) {
    "use strict";

    var r = n(2);
    e.exports = r.isStandardBrowserEnv() ? function () {
      return {
        write: function (e, t, n, o, i, s) {
          var a = [];
          a.push(e + "=" + encodeURIComponent(t)), r.isNumber(n) && a.push("expires=" + new Date(n).toGMTString()), r.isString(o) && a.push("path=" + o), r.isString(i) && a.push("domain=" + i), s === !0 && a.push("secure"), document.cookie = a.join("; ");
        },
        read: function (e) {
          var t = document.cookie.match(new RegExp("(^|;\\s*)(" + e + ")=([^;]*)"));
          return t ? decodeURIComponent(t[3]) : null;
        },
        remove: function (e) {
          this.write(e, "", Date.now() - 864e5);
        }
      };
    }() : function () {
      return {
        write: function () {},
        read: function () {
          return null;
        },
        remove: function () {}
      };
    }();
  }, function (e, t, n) {
    "use strict";

    var r = n(2);

    e.exports = function (e, t) {
      t = t || {};
      var n = {},
          o = ["url", "method", "params", "data"],
          i = ["headers", "auth", "proxy"],
          s = ["baseURL", "url", "transformRequest", "transformResponse", "paramsSerializer", "timeout", "withCredentials", "adapter", "responseType", "xsrfCookieName", "xsrfHeaderName", "onUploadProgress", "onDownloadProgress", "maxContentLength", "validateStatus", "maxRedirects", "httpAgent", "httpsAgent", "cancelToken", "socketPath"];
      r.forEach(o, function (e) {
        "undefined" != typeof t[e] && (n[e] = t[e]);
      }), r.forEach(i, function (o) {
        r.isObject(t[o]) ? n[o] = r.deepMerge(e[o], t[o]) : "undefined" != typeof t[o] ? n[o] = t[o] : r.isObject(e[o]) ? n[o] = r.deepMerge(e[o]) : "undefined" != typeof e[o] && (n[o] = e[o]);
      }), r.forEach(s, function (r) {
        "undefined" != typeof t[r] ? n[r] = t[r] : "undefined" != typeof e[r] && (n[r] = e[r]);
      });
      var a = o.concat(i).concat(s),
          u = Object.keys(t).filter(function (e) {
        return a.indexOf(e) === -1;
      });
      return r.forEach(u, function (r) {
        "undefined" != typeof t[r] ? n[r] = t[r] : "undefined" != typeof e[r] && (n[r] = e[r]);
      }), n;
    };
  }, function (e, t) {
    "use strict";

    function n(e) {
      this.message = e;
    }

    n.prototype.toString = function () {
      return "Cancel" + (this.message ? ": " + this.message : "");
    }, n.prototype.__CANCEL__ = !0, e.exports = n;
  }, function (e, t, n) {
    "use strict";

    function r(e) {
      if ("function" != typeof e) throw new TypeError("executor must be a function.");
      var t;
      this.promise = new Promise(function (e) {
        t = e;
      });
      var n = this;
      e(function (e) {
        n.reason || (n.reason = new o(e), t(n.reason));
      });
    }

    var o = n(23);
    r.prototype.throwIfRequested = function () {
      if (this.reason) throw this.reason;
    }, r.source = function () {
      var e,
          t = new r(function (t) {
        e = t;
      });
      return {
        token: t,
        cancel: e
      };
    }, e.exports = r;
  }, function (e, t) {
    "use strict";

    e.exports = function (e) {
      return function (t) {
        return e.apply(null, t);
      };
    };
  }]);
});
/**
 * Helper functions.
 */

const helpers = {
  insertAfter: (referenceNode, newNode) => {
    referenceNode.parentNode.insertBefore(newNode, referenceNode.nextSibling);
  },
  shuffle: array => {
    let counter = array.length;

    while (counter > 0) {
      let index = Math.floor(Math.random() * counter);
      counter--;
      let temp = array[counter];
      array[counter] = array[index];
      array[index] = temp;
    }

    return array;
  },
  handleize: string => {
    return string.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/-$/, '').replace(/^-/, '');
  },
  elementExists: selector => {
    return document.body.contains(document.querySelector(selector));
  }
};
/**
 * Classes
 */

const classes = {
  fieldError: 'field--error',
  isVisible: 'is-visible',
  isFixed: 'is-fixed',
  show: 'show',
  isMobileOnly: 'is-mobile-only',
  isDesktopOnly: 'is-desktop-only'
};
/**
 * Selectors.
 */

const selectors = {
  htmlEl: 'html',
  section: '.section',
  inputRadios: '.input-radio',
  countryInput: '[checkout="shipping-country"]',
  stateInput: '[checkout="shipping-state"]',
  orderIdInput: '[checkout="order-id"]',
  locationPopup: '[checkout="location-popup"]',
  modalCloseBtn: '.modal-close',
  marketingCheckbox: '#checkout_buyer_accepts_marketing',
  rememberMeCheckbox: '#checkout_remember_me',
  discounts: {
    container: '.order-summary__section--discount',
    input: '#checkout_reduction_code',
    invalidCode: '#error-for-reduction_code',
    ineligibleCode: '.notice--warning .notice__text strong'
  },
  shippingAddress: '.section--shipping-address',
  shippingMethodsContainer: '[data-shipping-methods]',
  shippingMethodHeader: '.section--shipping-method .section__header',
  shippingPhoneNumber: '[data-address-field="phone"]',
  shippingPhoneNumberInput: '#checkout_shipping_address_phone',
  shippingPhoneNumberError: '#error-for-phone',
  delayMessageNode: '.corona-shipping-delay',
  dynamicCheckoutButtons: '.dynamic-checkout__buttons',
  paymentMethodsContainer: '[data-payment-method]',
  orderSummaryToggle: '.order-summary-toggle',
  orderSummaryProductList: '.order-summary__section--product-list',
  prop65warningContainer: '[checkout="prop65-container"]',
  thankyouContainer: '[data-step="thank_you"]',
  thankyouContainer2: '[data-step="thank-you"]',
  thankyouContactLink: '.step__footer__info a',
  orderCheckContainer: '[checkout="check-container"]',
  orderCheckDevices: '[checkout="check-devices"]',
  orderCheckDevicesList: '[checkout="check-device-list"]',
  // existingCustomerDiscountContainer: '[checkout="existing-customer-discount-container"]',
  thankyouSomethingWrongButtons: '[checkout="something-wrong"]',
  discountCodeContainer: '.order-summary__section--discount',
  discountCodeContainerPaymentStep: '.section--reductions',
  mentionMeWrapper: '#mmWrappercheckout',
  mentionMeWrapper2: '#mmWrappercheckout2'
};
/**
 * Global vars.
 */

let nodes = {};
let checkoutStepNum = 1;
/**
 * Setup the node selectors.
 */

const setNodeSelectors = () => {
  nodes = {
    htmlEl: document.querySelector(selectors.htmlEl) || null,
    inputRadios: Array.from(document.querySelectorAll(selectors.inputRadios)) || null,
    countryInput: document.querySelector(selectors.countryInput) || null,
    stateInput: document.querySelector(selectors.stateInput) || null,
    orderIdInput: document.querySelector(selectors.orderIdInput) || null,
    marketingCheckbox: document.querySelector(selectors.marketingCheckbox) || null,
    rememberMeCheckbox: document.querySelector(selectors.rememberMeCheckbox) || null,
    discountContainer: document.querySelector(selectors.discounts.container) || null,
    shippingMethodsContainer: document.querySelector(selectors.shippingMethodsContainer) || null,
    shippingMethodHeader: document.querySelector(selectors.shippingMethodHeader) || null,
    shippingPhoneNumber: document.querySelector(selectors.shippingPhoneNumber) || null,
    shippingPhoneNumberInput: document.querySelector(selectors.shippingPhoneNumberInput) || null,
    delayMessageNode: document.querySelector(selectors.delayMessageNode) || null,
    dynamicCheckoutButtons: document.querySelector(selectors.dynamicCheckoutButtons) || null,
    paymentMethodsContainer: document.querySelector(selectors.paymentMethodsContainer) || null,
    orderSummaryToggle: document.querySelector(selectors.orderSummaryToggle) || null,
    orderSummaryProductList: document.querySelector(selectors.orderSummaryProductList) || null,
    prop65warningContainer: document.querySelector(selectors.prop65warningContainer) || null,
    // the data-step value changes on refresh from thank_you to thank-you because Shopify is awful.
    thankyouContainer: document.querySelector(selectors.thankyouContainer) || document.querySelector(selectors.thankyouContainer2) || null,
    orderCheckContainer: document.querySelector(selectors.orderCheckContainer) || null,
    orderCheckDevicesList: document.querySelector(selectors.orderCheckDevicesList) || null,
    // existingCustomerDiscountContainer: document.querySelector(selectors.existingCustomerDiscountContainer) || null,
    discountCodeContainer: document.querySelector(selectors.discountCodeContainer) || null,
    discountCodeContainerPaymentStep: document.querySelector(selectors.discountCodeContainerPaymentStep) || null
  };
};
/**
 * Toggle a class to prevent scrolling.
 */


const bodyFixed = arg => {
  if (arg === 'unlock') {
    nodes.htmlEl.classList.remove(classes.isFixed);

    document.ontouchmove = () => {
      return true;
    };
  } else if (arg === 'toggle') {
    nodes.htmlEl.classList.toggle(classes.isFixed);
  } else {
    nodes.htmlEl.classList.add(classes.isFixed);

    document.ontouchmove = event => {
      event.preventDefault();
    };
  }
};
/**
 * Open the modal.
 */


const modalOpen = modal => {
  modal.classList.add(classes.show);
  bodyFixed();
};
/**
 * Close the modal.
 */


const modalClose = el => {
  const modal = el.closest('.modal');
  modal.classList.remove(classes.show);
  document.querySelectorAll(['html', 'body']).forEach(el => {
    el.classList.remove(classes.isFixed);
  });
};
/**
 * Initialise the checkout scripts.
 *
 * @param {String} pageEvent the Shopify page event that's been fired
 */


const checkoutInit = pageEvent => {
  const shopifyCheckoutStep = window.Shopify.Checkout.step;
  setNodeSelectors();
  setChangeListeners();

  switch (shopifyCheckoutStep) {
    case 'contact_information':
      checkoutStepNum = 1;
      dataLayerCheckoutStep(pageEvent);
      changeRegionPopup();
      marketingCheckbox();
      addLatinCharacterNote();
      characterLimitOnAddress();
      showAttributionSection();
      invalidDiscountCode(pageEvent);
      addExpressCheckoutText();
      limitPhoneNumber();
      injectMentionMeReferLink();
      break;

    case 'shipping_method':
      checkoutStepNum = 2;
      californianProp65Warning(pageEvent);
      dataLayerCheckoutStep(pageEvent);
      checkForRadioContainer(pageEvent);
      invalidDiscountCode(pageEvent);
      setShippingCodeCookie().then(() => {
        addShippingDelayMsg();
      });
      injectMentionMeReferLink();
      break;

    case 'payment_method':
      checkoutStepNum = 3;
      californianProp65Warning(pageEvent);
      dataLayerCheckoutStep(pageEvent);
      checkForRadioContainer(pageEvent);
      invalidDiscountCode(pageEvent);
      injectMentionMeReferLink();
      break;

    case 'thank_you':
      checkoutStepNum = 4;
      dataLayerThankYou();
      updateContactLink(); // showExistingCustomerDiscountContainer()

      showOrderCheckContainer().then(() => {
        updateSomethingWrongLink();
      });
      break;

    case 'processing':
      break;

    default:
      checkoutStepNum = 4;
      updateContactLink(); // showExistingCustomerDiscountContainer()

      showOrderCheckContainer();
  }
};

const setChangeListeners = () => {
  if (nodes.inputRadios) {
    nodes.inputRadios.forEach(input => {
      input.addEventListener('change', () => {
        /**
         * Faking page:load as we need to run each time an input is changed but
         * shouldn't run on other Shopify events.
         */
        checkForRadioContainer('page:load');
      });
    });
  }

  if (nodes.shippingPhoneNumberInput) {
    nodes.shippingPhoneNumberInput.addEventListener('input', () => {
      toggleWarningPhoneLimit();
    });
  }
};
/**
 * Get the 2 letter code from the shipping address country dropdown.
 */


const getShippingAddressCode = () => {
  if (nodes.countryInput) {
    return nodes.countryInput.value;
  }
};
/**
 * Set the 2 letter country code as a cookie, set to expire 1 week later.
 */


const setShippingCodeCookie = () => {
  return new Promise(resolve => {
    const shippingCode = getShippingAddressCode();
    const today = new Date(Date.now());
    const nextWeek = new Date(today.getTime() + 7 * 24 * 60 * 60 * 1000);
    document.cookie = 'checkoutShippingCode=' + shippingCode + '; expires=' + nextWeek + '; path=/';
    resolve(shippingCode);
  });
};
/**
 * Add the shipping delay message to the form based on the cookie.
 * - Not needed on the UK store.
 * - EU has separate text from the rest of the stores.
 * - Certain countries in the EU have extended delays currently.
 * Add dispatch message below depending on location.
 */


const addShippingDelayMsg = () => {
  const location = window.theme.helpers.findCookie('checkoutShippingCode');
  const euDelayedCodes = ['BG', 'CZ', 'DK', 'DE', 'EE', 'EL', 'ES', 'FR', 'HR', 'IT', 'CY', 'LV', 'LT', 'LU', 'HU', 'MT', 'NL', 'AT', 'PL', 'PT', 'RO', 'SI', 'SK', 'FI', 'SE'];
  let delayMsg;
  let dispatchMsg;

  if (!window.theme.helpers.ukArray.includes(location)) {
    if (window.theme.helpers.detectStore === 'EU' || window.theme.helpers.detectStore === 'FR' || window.theme.helpers.detectStore === 'ES' || window.theme.helpers.detectStore === 'DEV') {
      if (euDelayedCodes.includes(location)) {
        delayMsg = window.theme.strings.checkout.shippingDelay.euDelayed;
      } else {
        delayMsg = window.theme.strings.checkout.shippingDelay.euGeneric;
      }
    }
  } else if (location === 'GB') {
    delayMsg = window.theme.strings.checkout.shippingDelay.uk ? window.theme.strings.checkout.shippingDelay.uk : '';
  }

  if (location == 'US') {
    delayMsg = window.theme.strings.checkout.shippingDelay.us;
    dispatchMsg = window.theme.strings.checkout.dispatchTimes.us;
  } else if (window.theme.helpers.ukArray.includes(location)) {
    dispatchMsg = window.theme.strings.checkout.dispatchTimes.uk;
  } else if (window.theme.helpers.euArray.includes(location)) {
    dispatchMsg = window.theme.strings.checkout.dispatchTimes.eu;
  } else {
    dispatchMsg = window.theme.strings.checkout.dispatchTimes.row;
    delayMsg = window.theme.strings.checkout.shippingDelay.row;
  } // TODO find a better way to do this without using FALSE string


  if (delayMsg === 'FALSE') {
    delayMsg = '';
  }

  const delayHTML = delayMsg ? `<p class="shipping-messages__delay">${delayMsg}</p>` : '';
  const shippingMessageHTML = `
    <div class="shipping-messages">
      ${delayHTML}

      <p class="shipping-messages__dispatch">${dispatchMsg}</p>
    </div>
  `;
  nodes.shippingMethodHeader && nodes.shippingMethodHeader.insertAdjacentHTML('beforeend', shippingMessageHTML);
};
/**
 * Add a note to the shipping address to inform customers to
 * only enter 'English' characters (not needed for UK store).
 */


const addLatinCharacterNote = () => {
  if (window.theme.helpers.detectStore !== 'UK') {
    const addressFields = document.querySelector('[data-shipping-address] .section__content') || null;
    const noteEl = document.querySelector('[js-character-note]') || null;
    const noteHTML = `<div class="character-note" js-character-note>${window.theme.strings.checkout.latinCharsNote}</div>`;

    if (noteEl) {
      return;
    }

    if (addressFields) {
      addressFields.insertAdjacentHTML('beforebegin', noteHTML);
    }
  }
};
/**
 * Add text and logic for changing region popup.
 */


const changeRegionPopup = () => {
  const rememberMeContainer = nodes.rememberMeCheckbox ? nodes.rememberMeCheckbox.closest('.field--show-floating-label') : null;

  if (rememberMeContainer) {
    rememberMeContainer.insertAdjacentHTML('beforebegin', `
        <div class="field field--optional">
          <div class="field__input-wrapper">
            <h3 style="margin-top: 10px;">Don\'t see your country listed?</h3>
            <p>We have multiple stores to serve different regions. You may need to go to one of our other stores to find your shipping country. To check which regions we serve <a href="#" data-popup="#location-switch" popup="trigger">click here.</a></p>
          </div>
        </div>
      `);
    document.querySelector('[popup="trigger"]').addEventListener('click', event => {
      event.preventDefault();
      const modal = document.querySelector(event.target.dataset.popup);
      modalOpen(modal);
    });
    document.querySelector(selectors.modalCloseBtn).addEventListener('click', event => {
      modalClose(event.target);
    });
  }
};
/**
 * Marketing checkbox logic.
 */


const marketingCheckbox = () => {
  const usrLocation = window.theme.helpers.findCookie('usrLocation');
  const acceptsMarketing = window.theme.helpers.euArray.includes(usrLocation);

  if (nodes.marketingCheckbox && acceptsMarketing) {
    nodes.marketingCheckbox.checked = true;
  }
};
/**
 * Limit number of characters on address field.
 */


const characterLimitOnAddress = () => {
  if (document.querySelector('#checkout_shipping_address_address1') != null) {
    document.querySelector('#checkout_shipping_address_address1').setAttribute('maxLength', 30);
  }
};
/**
 * Check whether a customer has entered an
 * invalid/ineligible discount code
 */


const invalidDiscountCode = pageEvent => {
  const cont = nodes.discountContainer;
  const {
    input,
    invalidCode: invalid,
    ineligibleCode: ineligible
  } = selectors.discounts;
  let code;

  if (!cont) {
    return;
  }

  if (cont.querySelector(invalid)) {
    code = cont.querySelector(input).value;
    changeInvalidDiscountCodeMessage(cont, invalid);
  } else if (cont.querySelector(ineligible)) {
    code = cont.querySelector(ineligible).innerText;
  }

  if (!code) {
    return;
  }

  dataLayerInvalidDiscountCode(pageEvent, code);
};
/**
 * Change the invalid discount code message
 * to the one in the language file
 *
 * @param {HTMLSelector} cont DOM object for the order summary discount wrapper
 * @param {HTMLSelector} invalid DOM object for the discount code error message
 */


const changeInvalidDiscountCodeMessage = (cont, invalid) => {
  const elErrorMessage = cont.querySelector(invalid);
  elErrorMessage.innerHTML = window.theme.strings.checkout.invalidDiscountCodeMessage;
  elErrorMessage.classList.add(classes.show);
};
/**
 * Update the contact link on the thank you page to link
 * to the contact page instead of a mailto link.
 */


const updateContactLink = () => {
  nodes.thankyouContainer.querySelector(selectors.thankyouContactLink).setAttribute('href', window.theme.strings.checkout.contactUsLink);
};
/**
 * Clone the hidden compatibility section and insert into the right
 * part of the thank you and add a class so it's visible.
 * - If the device list has children, show the device compatibility section
 */


const showOrderCheckContainer = () => {
  return new Promise(resolve => {
    const compatClone = nodes.orderCheckContainer.cloneNode(true);
    if (devicesListItemCount()) compatClone.querySelector(selectors.orderCheckDevices).classList.add(classes.isVisible);
    compatClone.classList.add(classes.isVisible);
    helpers.insertAfter(nodes.thankyouContainer.querySelector(selectors.section), compatClone);
    resolve(compatClone);
  });
};
/**
 * Update the something wrong link on the thank you page
 * to include query string to pre-fill contact form.
 */


const updateSomethingWrongLink = () => {
  const orderNumber = nodes.orderIdInput.value.replace('#', '');
  const customerEmail = window.Shopify.checkout.email;
  const customerName = window.Shopify.checkout.billing_address.first_name + ' ' + window.Shopify.checkout.billing_address.last_name;
  nodes.thankyouSomethingWrongButtons = Array.from(document.querySelectorAll(selectors.thankyouSomethingWrongButtons)) || null;
  nodes.thankyouSomethingWrongButtons.forEach(somethingWrongButton => {
    let somethingWrongLink = somethingWrongButton.getAttribute('href');
    somethingWrongLink += `?reason=cancel-order&email=${customerEmail}&name=${customerName}&ordernum=${orderNumber}`;
    somethingWrongButton.setAttribute('href', somethingWrongLink);
  });
};
/**
 * Count the number of children the device list element has.
 */


const devicesListItemCount = () => {
  return nodes.orderCheckDevicesList.childElementCount;
};
/**
 * Check if they are an existing customer
 * if so show them a discount code
 */


const showExistingCustomerDiscountContainer = () => {
  const custDiscountClone = nodes.existingCustomerDiscountContainer.cloneNode(true);
  custDiscountClone.classList.add(classes.isVisible);
  helpers.insertAfter(nodes.thankyouContainer.querySelector('.section'), custDiscountClone);
};
/**
 * Show an attribution dropdown on the shipping step so we can capture how customers found us.
 */


const showAttributionSection = () => {
  const {
    title,
    pleaseSelectOption,
    options,
    otherOption
  } = window.theme.strings.checkout.howDidYouHear;
  const randomisedOptions = helpers.shuffle(options);
  let attributionHtml = `
    <div class="section section--attribution">
      <div class="section__header">
        <h2 class="section__title">${title}</h2>
      </div>

      <div class="select-container">
        <select
          class="field__input field__input--select"
          name="checkout[attributes][Attribution]"
        >
          <option value="">${pleaseSelectOption}</option>
  `;
  randomisedOptions.forEach(option => {
    attributionHtml += `
      <option value="${helpers.handleize(option)}">${option}</option>
    `;
  });
  attributionHtml += `<option value="${helpers.handleize(otherOption)}">${otherOption}</option>`;
  attributionHtml += `
        </select>

        <svg class="icon icon--caret-down" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 10 10"><path d="M0 3h10L5 8"/></svg>
      </div>
    </div>
  `;
  document.querySelector(selectors.shippingAddress).insertAdjacentHTML('afterend', attributionHtml);
};
/**
 * Check to see if express checkout buttons exist. If so,
 * add text below to warn customers of redirect.
 */


const addExpressCheckoutText = () => {
  if (nodes.dynamicCheckoutButtons) {
    const expressText = `
      <div class="dynamic-checkout__redirect-text">
        ${window.theme.strings.checkout.expressCheckoutRedirectText}
      </div>
    `;
    nodes.dynamicCheckoutButtons.insertAdjacentHTML('afterend', expressText);
  } else {
    setTimeout(addExpressCheckoutText, 250);
  }
};
/**
 * Add attribute to phone number field to limit it to 15 chars.
 */


const limitPhoneNumber = () => {
  nodes.shippingPhoneNumberInput.setAttribute('maxlength', 15);
};
/**
 * Toggle warning on phone number field about number limit.
 * Check if error is already present, if so, update textContent
 * if not, add HTML.
 */


const toggleWarningPhoneLimit = () => {
  const phoneNumberError = nodes.shippingPhoneNumber.querySelector(selectors.shippingPhoneNumberError) || null;

  if (nodes.shippingPhoneNumberInput.value.length >= 15) {
    nodes.shippingPhoneNumber.setAttribute('data-limit-error', '');

    if (phoneNumberError) {
      phoneNumberError.textContent = window.theme.strings.checkout.shippingPhoneNumberLimit;
    } else {
      const errorHTML = `
        <p class="field__message field__message--error" id="error-for-phone">
          ${window.theme.strings.checkout.shippingPhoneNumberLimit}
        </p>
      `;
      nodes.shippingPhoneNumber.insertAdjacentHTML('beforeend', errorHTML);
    }
  } else {
    nodes.shippingPhoneNumber.removeAttribute('data-limit-error');
  }
};
/**
 * Push datalayer event based on step in checkout.
 * - First step requires products so using Axios to obtain cart data.
 * @param {Number} step
 */


const dataLayerCheckoutStep = pageEvent => {
  if (pageEvent !== 'page:load') {
    return;
  }

  window.dataLayer = window.dataLayer || [];
  const dataLayerData = {
    event: 'checkout',
    ecommerce: {
      currencyCode: window.theme.shopCurrencyISO,
      checkout: {
        actionField: {
          step: checkoutStepNum
        },
        products: []
      }
    }
  };
  axios.get('/cart.js').then(({
    data
  }) => {
    data.items.forEach(item => {
      const prod = {
        id: item.sku,
        name: item.product_title,
        brand: item.vendor,
        category: item.product_type,
        variant: item.variant_title,
        price: item.final_price / 100,
        quantity: item.quantity
      }; // Check for custom line property and make sure it equals the final_price value.
      // If a customer adds an accessory during upsell then removes the case, the property would still
      // exist but wouldn't match the price.

      if (item.properties._finalPrice && item.properties._finalPrice == item.final_price) {
        prod.bundled = true;
        prod.unbundledPrice = item.price / 100;
      }

      dataLayerData.ecommerce.checkout.products.push(prod);
    }); // store product array in localStorage so it can be retrieved at thank you page.

    localStorage.setItem('cart_items', JSON.stringify(dataLayerData.ecommerce.checkout.products));
  });
  window.dataLayer.push(dataLayerData);
};
/**
 * Send the invalid/ineligible discount code to the dataLayer.
 *
 * @param {String} code The invalid/ineligible code entered
 */


const dataLayerInvalidDiscountCode = (pageEvent, code) => {
  if (pageEvent !== 'page:change') {
    return;
  }

  window.dataLayer = window.dataLayer || [];
  window.dataLayer.push({
    event: 'invalidDiscountCode',
    usedCode: code
  });
};
/**
 * Check to see if radio container exists before running checkoutOption function.
 */


const checkForRadioContainer = pageEvent => {
  if (pageEvent !== 'page:load') {
    return;
  }

  const container = nodes.shippingMethodsContainer || nodes.paymentMethodsContainer;

  function doesContainerExist() {
    if (container) {
      dataLayerCheckoutOption(container);
    } else {
      setTimeout(doesContainerExist, 250);
    }
  }

  doesContainerExist();
};
/**
 * Display a warning if the customer is shipping to California.
 */


const californianProp65Warning = pageEvent => {
  if (pageEvent !== 'page:load') {
    return;
  }

  if (nodes.stateInput.value === 'CA') {
    const prop65CloneMobile = nodes.prop65warningContainer.cloneNode(true);
    const prop65CloneDesktop = nodes.prop65warningContainer.cloneNode(true);
    prop65CloneMobile.classList.add(classes.isVisible, classes.isMobileOnly);
    prop65CloneDesktop.classList.add(classes.isVisible, classes.isDesktopOnly); // mobile element doesn't exist on page load so needs to be inside setTimeout

    setTimeout(() => {
      // needs to be added in two places for mobile/desktop
      nodes.orderSummaryToggle.parentNode.insertBefore(prop65CloneMobile, nodes.orderSummaryToggle);
      nodes.orderSummaryProductList.parentNode.insertBefore(prop65CloneDesktop, nodes.orderSummaryProductList);
    });
  }
};
/**
 * Inject MentionMe DIVs for friend referral popup.
 * - On payment section there are 2 discount code sections so it needs to add multiple DIVs.
 */


const injectMentionMeReferLink = () => {
  if (window.MentionMeFiredTags != undefined && window.MentionMeFiredTags != '') {
    const situation = 'checkout2';
    const implementation = 'link';

    if (window.MentionMeFiredTags[implementation + situation] == true) {
      delete window.MentionMeFiredTags[implementation + situation];
    }
  }

  if (nodes.discountCodeContainer) {
    if (!helpers.elementExists(selectors.mentionMeWrapper)) {
      const referFriendContainer = document.createElement('div');
      const mmScript = document.createElement('script');
      referFriendContainer.id = 'mmWrappercheckout';
      referFriendContainer.classList.add('order-summary__refer-friend');
      nodes.discountCodeContainer.parentNode.insertBefore(referFriendContainer, nodes.discountCodeContainer.nextSibling);
      mmScript.src = 'https://tag.mention-me.com/api/v2/refereefind/mme528be9d?situation=checkout';
      document.head.appendChild(mmScript);
    }

    if (nodes.discountCodeContainerPaymentStep) {
      if (!helpers.elementExists(selectors.mentionMeWrapper2)) {
        const referFriendContainer2 = document.createElement('div');
        const mmScript2 = document.createElement('script');
        referFriendContainer2.id = 'mmWrappercheckout2';
        referFriendContainer2.classList.add('order-summary__refer-friend', 'hidden-on-desktop');
        nodes.discountCodeContainerPaymentStep.parentNode.insertBefore(referFriendContainer2, nodes.discountCodeContainerPaymentStep.nextSibling);
        mmScript2.src = 'https://tag.mention-me.com/api/v2/refereefind/mme528be9d?situation=checkout2';
        document.head.appendChild(mmScript2);
      }
    }
  }
};
/**
 * Push dataLayer event for the selected option at shipping & payment steps.
 * - The option labels are nested differently for both steps and different again on payment
 * depending on if the payment method is text or an image.
 */


const dataLayerCheckoutOption = container => {
  const selectedOption = container.querySelector(`${selectors.inputRadios}:checked`); // if all options get deselected (happens when applying discounts), then return.

  if (!selectedOption) {
    return;
  }

  const labelContainer = selectedOption.parentNode.parentNode.querySelector('label');
  let labelText;

  if (checkoutStepNum === 3) {
    labelText = labelContainer.querySelector('.visually-hidden') || labelContainer;
  } else {
    labelText = labelContainer.querySelector('[data-shipping-method-label-title]');
  }

  const option = labelText.innerText;
  window.dataLayer = window.dataLayer || [];
  window.dataLayer.push({
    event: 'checkoutOption',
    ecommerce: {
      currencyCode: window.theme.shopCurrencyISO,
      checkout_option: {
        actionField: {
          step: checkoutStepNum,
          option
        }
      }
    }
  });
};
/**
 * Push a purchase event to the dataLayer on thank you page.
 */


const dataLayerThankYou = () => {
  const storedProducts = JSON.parse(localStorage.getItem('cart_items')) || [];
  const {
    total_price,
    shipping_rate,
    total_tax,
    discount
  } = window.Shopify.checkout;
  window.dataLayer = window.dataLayer || [];
  window.dataLayer.push({
    event: 'purchase',
    ecommerce: {
      currencyCode: window.theme.shopCurrencyISO,
      purchase: {
        actionField: {
          id: nodes.orderIdInput.value,
          affiliation: window.Shopify.shop,
          revenue: +total_price,
          shipping: +shipping_rate.price,
          tax: +total_tax,
          coupon: discount ? discount.code : ''
        },
        products: storedProducts
      }
    }
  });
};
/**
 * Shopify checkout events.
 */


const shopifyEvents = ['page:load', 'page:change', 'page:update'];
shopifyEvents.forEach(event => {
  document.addEventListener(event, () => {
    checkoutInit(event);
  });
});